{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "CodeLLDB: 활성 파일 디버그",
      "type": "lldb",
      "request": "launch",
      "program": "${fileDirname}/${fileBasenameNoExtension}",
      "args": [],
      "cwd": "${fileDirname}",
      "console": "integratedTerminal",
      "preLaunchTask": "C/C++: clang++ 활성 파일 빌드"
    }
  ]
}
